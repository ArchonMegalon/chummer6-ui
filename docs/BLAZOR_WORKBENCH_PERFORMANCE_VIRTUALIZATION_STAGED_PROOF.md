# Blazor Workbench Performance-Virtualization Staged Proof

## Purpose

This source-staged proof keeps large-sheet performance and virtualization affordances visible on the promoted Blazor workbench route.

The browser client should expose lazy sections, virtual lists, render budget, memory posture, degraded mode, and profiler cues so dense Chummer sheets can scale toward desktop-class workloads.

## Source-Staged Scope

The staged performance-virtualization lane covers:

- a performance/virtualization strip on the promoted workbench route
- lazy sections, virtual lists, render budget, memory posture, degraded mode, and profiler shortcuts
- scoped responsive styling for desktop and mobile browser use

## Source Check

```bash
bash scripts/ai/milestones/blazor-workbench-performance-virtualization-staged-proof-check.sh
```

Expected receipt:

```text
.codex-studio/published/BLAZOR_WORKBENCH_PERFORMANCE_VIRTUALIZATION_STAGED_PROOF.generated.json
```

## Boundary

This is source alignment only. It proves that promoted workbench performance-virtualization source, style, status reporting, and docs agree.

It is not hosted browser execution proof, Docker self-host proof, virtualized-rendering proof, lazy-loading proof, memory-control proof, profiler proof, route-click proof, screenshot proof, or desktop-equivalent workflow parity.
