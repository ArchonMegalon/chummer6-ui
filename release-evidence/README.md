# Release evidence archive

This directory contains immutable, release-versioned evidence captured from
hosted verification runs. A receipt here proves only the scope declared by its
adjacent `evidence-index.json`.

These files are not release authority and are not a `CURRENT` pointer. They do
not promote a channel, activate an artifact, or assert whole-product preview,
stable, or flagship readiness. Registry and Design remain the authorities for
those decisions.

