<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Chummer.Avalonia.Controls.CharacterCareerClassicPort">
  <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
    <Grid RowDefinitions="Auto,Auto,Auto,*"
          RowSpacing="10">
      <TextBlock x:Name="CareerNoticeText"
                 TextWrapping="Wrap"
                 Opacity="0.8" />
      <WrapPanel x:Name="CareerTabsPanel"
                 Grid.Row="1"
                 Orientation="Horizontal" />
      <WrapPanel x:Name="CareerFactsPanel"
                 Grid.Row="2"
                 Orientation="Horizontal" />
      <Grid Grid.Row="3"
            ColumnDefinitions="3*,3*,2*"
            ColumnSpacing="10">
        <StackPanel x:Name="CareerSummaryPanel"
                    Spacing="10" />
        <StackPanel x:Name="CareerInventoryPanel"
                    Grid.Column="1"
                    Spacing="10" />
        <StackPanel x:Name="CareerActionsPanel"
                    Grid.Column="2"
                    Spacing="10" />
      </Grid>
    </Grid>
  </ScrollViewer>
</UserControl>
