<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Chummer.Avalonia.Controls.SectionHostControl">
  <Grid RowDefinitions="Auto,*,Auto" RowSpacing="12">
    <Border Grid.Row="0" Classes="shell-card subtle">
      <StackPanel Spacing="6">
        <TextBlock Classes="shell-kicker" Text="Section Briefing" />
        <TextBlock x:Name="NoticeText" Classes="shell-section-title" Text="Notice: Ready." TextWrapping="Wrap" />
      </StackPanel>
    </Border>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <Border Classes="shell-card">
      <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,*,Auto,*" RowSpacing="6">
        <TextBlock Grid.Row="0" Text="Active Section Preview" Classes="shell-section-title" />
        <Border Grid.Row="1"
                x:Name="BuildLabBorder"
                Classes="shell-card"
                IsVisible="False"
                Padding="8"
                BorderThickness="1"
                Background="{DynamicResource ChummerShellSurfaceBrush}"
                BorderBrush="{DynamicResource ChummerShellBorderBrush}">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" RowSpacing="6">
            <TextBlock x:Name="BuildLabSummaryText"
                       Text="Build Lab intake"
                       FontWeight="Bold"
                       TextWrapping="Wrap" />
            <ListBox Grid.Row="1" x:Name="BuildLabFieldsList" Height="132">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <ListBox Grid.Row="2" x:Name="BuildLabConstraintsList" Height="72">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <TextBox Grid.Row="3"
                     x:Name="BuildLabProvenanceBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <ListBox Grid.Row="4" x:Name="BuildLabVariantsList" Height="128">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <TextBox Grid.Row="5"
                     x:Name="BuildLabCoverageBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <TextBox Grid.Row="6"
                     x:Name="BuildLabTimelinesBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <TextBox Grid.Row="7"
                     x:Name="BuildLabExportPayloadsBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <ListBox Grid.Row="8" x:Name="BuildLabExportTargetsList" Height="96">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <ListBox Grid.Row="9" x:Name="BuildLabActionsList" Height="56">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
          </Grid>
        </Border>
        <Border Grid.Row="2"
                x:Name="BrowseWorkspaceBorder"
                Classes="shell-card"
                IsVisible="False"
                Padding="8"
                BorderThickness="1"
                Background="{DynamicResource ChummerShellSurfaceBrush}"
                BorderBrush="{DynamicResource ChummerShellBorderBrush}">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="6">
            <TextBlock x:Name="BrowseSummaryText"
                       Text="Browse workspace"
                       FontWeight="Bold"
                       TextWrapping="Wrap" />
            <ListBox Grid.Row="1" x:Name="BrowsePresetsList" Height="56">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <ListBox Grid.Row="2" x:Name="BrowseFacetsList" Height="96">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="8">
              <ListBox Grid.Column="0" x:Name="BrowseResultsList" Height="160">
                <ListBox.ItemsPanel>
                  <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                  </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
              </ListBox>
              <TextBox Grid.Column="1"
                       x:Name="BrowseDetailBox"
                       AcceptsReturn="True"
                       TextWrapping="Wrap"
                       IsReadOnly="True" />
            </Grid>
            <ListBox Grid.Row="4" x:Name="BrowseSelectedItemsList" Height="76">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
          </Grid>
        </Border>
        <Border Grid.Row="3"
                x:Name="ContactGraphBorder"
                Classes="shell-card"
                IsVisible="False"
                Padding="8"
                BorderThickness="1"
                Background="{DynamicResource ChummerShellSurfaceBrush}"
                BorderBrush="{DynamicResource ChummerShellBorderBrush}">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" RowSpacing="6">
            <TextBlock x:Name="ContactGraphSummaryText"
                       Text="Contact relationship graph"
                       FontWeight="Bold"
                       TextWrapping="Wrap" />
            <ListBox Grid.Row="1" x:Name="ContactNodeList" Height="112">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <TextBox Grid.Row="2"
                     x:Name="ContactFactionStatusBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <TextBox Grid.Row="3"
                     x:Name="ContactHeatObligationBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <TextBox Grid.Row="4"
                     x:Name="ContactFavorRailBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
          </Grid>
        </Border>
        <Border Grid.Row="4"
                x:Name="NpcPersonaStudioBorder"
                Classes="shell-card"
                IsVisible="False"
                Padding="8"
                BorderThickness="1"
                Background="{DynamicResource ChummerShellSurfaceBrush}"
                BorderBrush="{DynamicResource ChummerShellBorderBrush}">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" RowSpacing="6">
            <TextBlock x:Name="NpcPersonaSummaryText"
                       Text="NPC Persona Studio"
                       FontWeight="Bold"
                       TextWrapping="Wrap" />
            <ListBox Grid.Row="1" x:Name="NpcPersonaList" Height="92">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <ListBox Grid.Row="2" x:Name="NpcPolicyList" Height="92">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <TextBox Grid.Row="3"
                     x:Name="NpcEvidenceBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <TextBox Grid.Row="4"
                     x:Name="NpcApprovalBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
          </Grid>
        </Border>
        <Border Grid.Row="5"
                x:Name="DowntimePlannerBorder"
                Classes="shell-card"
                IsVisible="False"
                Padding="8"
                BorderThickness="1"
                Background="{DynamicResource ChummerShellSurfaceBrush}"
                BorderBrush="{DynamicResource ChummerShellBorderBrush}">
          <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="6">
            <TextBlock x:Name="DowntimePlannerSummaryText"
                       Text="Downtime planner"
                       FontWeight="Bold"
                       TextWrapping="Wrap" />
            <ListBox Grid.Row="1" x:Name="DowntimePlannerLanesList" Height="92">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
            <TextBox Grid.Row="2"
                     x:Name="DowntimeCalendarBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     IsReadOnly="True" />
            <ListBox Grid.Row="3" x:Name="DowntimeScheduleList" Height="96">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
            </ListBox>
          </Grid>
        </Border>
        <ListBox Grid.Row="6" x:Name="SectionRowsList" Height="120">
          <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
              <VirtualizingStackPanel />
            </ItemsPanelTemplate>
          </ListBox.ItemsPanel>
        </ListBox>
        <TextBox Grid.Row="7"
                 x:Name="SectionPreviewBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 IsReadOnly="True" />
      </Grid>
      </Border>
    </ScrollViewer>

    <Border Grid.Row="2" Classes="shell-card subtle">
      <StackPanel Spacing="4">
        <TextBlock Classes="shell-kicker" Text="Developer Utility" />
        <TextBlock Text="Raw XML Import" Classes="shell-section-title" />
        <TextBox x:Name="XmlInputBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="100"
                 Watermark="Optional raw XML payload for diagnostics" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
